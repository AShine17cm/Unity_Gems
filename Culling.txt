Unity.Jobs
Unity.Collections

NativeArray<>  只要是值类型的结构体就行

缩减 Rect的半径，将两个多边形的包含，转化为一个多边形和点之间的包含

将 遮挡物的 四个点 和 被遮挡物的中心 转换到 Screen空间

//如果 被遮挡物 在 Rect的投影内，Rect 投影的对角点 內积必然都小于0
//反之 至少有一个 对角点 內积大于0

NativeArray<OccluderData>  赋值

IJob.Execute

IJob.Schedule

JobHanlde.Complete